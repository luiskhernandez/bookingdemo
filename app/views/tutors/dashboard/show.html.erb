<div class="min-h-screen bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-4xl mx-auto">
    <h1 class="text-3xl font-bold text-gray-900 mb-8">Tutor Dashboard</h1>
    
    <div class="bg-white shadow rounded-lg p-6">
      <h2 class="text-xl font-semibold mb-4">Welcome, <%= current_user.tutor.display_name %>!</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="bg-blue-50 p-4 rounded-lg">
          <h3 class="font-medium text-blue-900 mb-2">Your Profile</h3>
          <p class="text-sm text-blue-700 mb-2"><strong>Display Name:</strong> <%= current_user.tutor.display_name %></p>
          <p class="text-sm text-blue-700 mb-2"><strong>Timezone:</strong> <%= current_user.tutor.timezone %></p>
          <p class="text-sm text-blue-700"><strong>Bio:</strong> <%= current_user.tutor.bio %></p>
        </div>
        
        <div class="bg-green-50 p-4 rounded-lg">
          <h3 class="font-medium text-green-900 mb-2">Your Lesson Types</h3>
          <% current_user.tutor.lesson_types.each do |lesson_type| %>
            <div class="text-sm text-green-700 mb-2">
              <strong><%= lesson_type.title %></strong> - <%= lesson_type.duration_minutes %> minutes
              <% if lesson_type == current_user.tutor.default_lesson_type %>
                <span class="bg-green-200 px-2 py-1 rounded-full text-xs">Default</span>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
      
      <div class="mt-6">
        <p class="text-gray-600">This is your tutor dashboard. More features will be added in future milestones.</p>
      </div>
    </div>
  </div>
</div>
